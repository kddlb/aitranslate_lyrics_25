name: aitranslate_lyrics_25
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.4

dependencies:
  dart_openai: ^5.1.0
  flutter:
    sdk: flutter
  flutter_settings_screens: ^0.3.4
  flutter_widget_from_html: ^0.15.3
  shared_preferences: ^2.3.3
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
